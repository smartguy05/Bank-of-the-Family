// Root build file. Plugins are declared here (with apply false) and applied in app/build.gradle.
plugins {
    id "com.android.application" version "8.7.2" apply false
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
